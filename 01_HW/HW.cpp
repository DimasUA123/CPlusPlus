#include <iostream>
using namespace std;

void main(){

	// Завдання 1
	/*float A[3], sum = 0;
	for (int i = 0; i < 3; i++) {
		cout << "Enter " << i+1 << " number : ";
		cin >> A[i];
		sum += A[i];
	}
	cout << "\nFirst number - " << A[0] << "; Second number - " << A[1] << "; Third number - " << A[2] << "\nSumma - " << sum;*/



	// Завдання 2
	/*float A[2];
	for (int i = 0; i < 2; i++) {
		cout << "Enter " << i + 1 << " number : ";
		cin >> A[i];
	}
	cout << "\nResult - " << (A[0]+A[1])/2;*/



	// Завдання 3
	/*float a;
	cout << "Enter distance(km) : ";
	cin >> a;
	cout << "\nDistance in meters = " << a * 1000 << "m";*/



	// Завдання 4
	/*const int priceOfTV = 22499, priceOfPC = 60000, priceOfLaptop = 34999, priceOfMouse = 2300;
	int numTV, numPC, numLaptop, numMouse, fullPrice;

	cout << "Enter number of TV, PC, Laptop, Mouse\n";
	cin >> numTV >> numPC >> numLaptop >> numMouse;

	fullPrice = priceOfTV * numTV + priceOfPC * numPC + priceOfLaptop * numLaptop + priceOfMouse * numMouse;
	cout << "\nFull price = " << fullPrice;*/
	
	

	// Завдання 5
	/*float a;
	cout << "Enter any number : ";
	cin >> a;
	cout << "\nSquare of number = " << a*a;*/



	// Завдання 6
	/*const int hoursInDays = 24, minuteInHour = 60;
	int a;
	cout << "Enter number of days : ";
	cin >> a;
	cout << "\nMinute in " << a << " days = " << a * hoursInDays * minuteInHour;*/
}